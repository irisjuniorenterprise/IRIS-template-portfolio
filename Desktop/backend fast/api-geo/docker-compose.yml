# docker-compose.yml
version: '3.8'

services:
  tunisia-api:
    build: .
    container_name: tunisia-governorates-api
    ports:
      - "8000:8000"
    restart: unless-stopped
    volumes:
      - ./tunisia.geojson:/app/tunisia.geojson:ro
    environment:
      - PYTHONUNBUFFERED=1